{$APP_DOMAIN} {
      encode gzip

      # reverse_proxy transparently handles WebSocket upgrades and sets
      # X-Forwarded-For / X-Forwarded-Proto (https) for the backend.
      reverse_proxy frontend:80
}